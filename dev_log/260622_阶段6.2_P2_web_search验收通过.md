# 阶段 6.2 — P2 web_search 验收通过

**日期**：2026-06-22
**模式**：Mode F 验收
**内容**：Tavily 搜索链路端到端验证通过（CLI + 前端浏览器）

---

## 〇、做了什么

P2 验收 web_search 工具链路。代码之前已写好（`agent/tools/web_search.py`，exa_search + tavily_search），本次重点是跑起来验证。

---

## 一、验收结果

| 用例 | 路径 | intent | tool | 结果 |
|------|------|--------|------|------|
| 「今天有什么科技新闻？」 | CLI `run_graph_debug()` | `use_tool` | `tavily_search` | ✅ 返回 5 类科技新闻摘要 |
| 「那现在的 RTX 5090 多少钱？」 | 前端 Agent 开关 ON | — | — | ✅ 返回官方价/国内公版/非公版/溢价区间 |

**结论**：Tavily 搜索链路（路由 → 工具执行 → 结果合成 → SSE 输出）端到端通过。

---

## 二、未测项（不阻塞 P2）

- `exa_search`：`.env` 中 `EXA_API_KEY` 为空，代码已实现，待配 key 后补测
- `check_bound()` 接入 `tool_execute`：属加固项

---

## 三、下一步

P2 ✅，可进入路由优化或灵感引擎 Phase 1。



```text

建议做的事情：
根据 `上次会话成果.md` 和当前代码，进度可以这样看：

## 当前进度

```
路线1（文档摄入）✅ → 路线2（知识检索）✅ → 评估模块 ✅
FastAPI ✅ → 前端 ✅ → Docker ✅ → SQLite→PG ✅
Agent 骨架 ✅ → 记忆系统 ✅ → P1 /chat/graph ✅
```

阶段 6.2 里，**P0（记忆 CLI 验证）** 和 **P1（网页聊天走 Agent Graph）** 都已完成。

---

## 待办清单（按优先级）

| 优先级 | 任务 | 状态 | 说明 |
|--------|------|------|------|
| **P1.5** | 前端端到端验证记忆 | 🔄 部分完成 | 写入 OK；新 session 问偏好时路由器可能误判成 `rag_search` |
| **P2** | web_search 验收 | 🔄 代码已有 | `web_search.py` 已实现 `exa_search` + `tavily_search` 并注册，但**还没做过端到端验收** |
| P1.1 | 记忆关键词硬路由 | ❌ 不做 | 你已明确拒绝 |
| P3 | 清理 docstring 里的 `TODO(human)` | ⬜ 低优先级 | 不影响运行 |

---

## 建议的下一步（二选一）

### 选项 A：收尾 P1.5（推荐先做）

把记忆链路在**网页端**跑通一遍：

1. 启动：`uv run app.py`（Docker 4 容器保持运行）
2. 前端打开 **Agent Graph** 开关（走 `/chat/graph`）
3. 第一轮：告诉助理你的偏好（如「我喜欢小步教学」）
4. 开**新 session**，问「你还记得我的偏好吗？」
5. 看 `memory/MEMORY.md` 是否更新，以及回复是否用到记忆

已知问题：路由器可能把「我的偏好」判成 `rag_search` 而不是 `general_chat`。这是路由 prompt 的问题，不是 P1 的 bug；可以记下来，后面再调 `agent/router.py`。

### 选项 B：做 P2 验收（web_search）

代码已经写好，重点是**验证能跑**：

1. `.env` 里 `TAVILY_API_KEY` 已有；`EXA_API_KEY` 为空 → 先测 Tavily
2. CLI 快速测：

```bash
cd personal_assistant
PYTHONPATH=. uv run python -c "
from agent.main_graph import run_graph_debug
r = run_graph_debug('test-web', '今天有什么科技新闻？')
print('intent:', r.get('intent'))
print('tool:', r.get('target_tool'))
print('response:', r.get('final_response', '')[:500])
"
```

3. 通过后再在前端用 Agent Graph 问实时类问题（如「RTX 5090 多少钱」）
4. 验收通过后：`uv run python scripts/snapshot.py stage62_web_search "P2 web_search 验收通过"`

---

## 还没动的大方向

如果短期不想只做验收，可以往这些方向走（文档里提过）：

- **灵感引擎 Phase 1**：`inspire` 意图目前临时走单工具调用，后续要做发散检索管线
- **`check_bound()` 接入**：`tool_execute` 里还没调用，网络类工具上线时建议补上
- **`document_search`**：注册表里仍是 `func=None` 占位

---

**我的建议**：先做 **P1.5 前端记忆验收**（约 15 分钟），确认整条链路在网页上可用；然后做 **P2 web_search 验收**（Tavily 已有 key，上手快）。

你想从 P1.5 还是 P2 开始？我可以按你选的那条，用 Mode F 带你一步步做（规划 → 参考文件 → 验收）。

---
---
---

前端浏览器问题的结果：
问：那现在的RTX 5090 多少钱？

答：根据搜索结果，RTX 5090 的价格因版本和渠道差异较大：

官方首发价：1999 美元（约 1.46 万元人民币）
国内公版（FE）：约 33,999 元
国内非公版：约 24,399 元
部分渠道/黄牛价：甚至炒到 3,500 美元以上（约 2.5 万元+）
简单说，官方定价 1999 美元，但国内实际到手价普遍在 2.4 万～3.4 万元 之间，溢价严重。你是打算入手吗？

---

终端结果：
[root@iZbp12zrjuiewhsqxvgm4yZ assistant]# cd personal_assistant
[root@iZbp12zrjuiewhsqxvgm4yZ personal_assistant]# PYTHONPATH=. uv run python -c "
> from agent.main_graph import run_graph_debug
> r = run_graph_debug('test-web', '今天有什么科技新闻？')
> print('intent:', r.get('intent'))
> print('tool:', r.get('target_tool'))
> print('response:', r.get('final_response', '')[:500])
> "
intent: use_tool
tool: tavily_search
response: 今天有不少科技新闻，我帮你整理了几个重点：

1. **AI 与行业冲击**：京东刘强东称未来可能不需要快递员，计划送70万蓝领兄弟去培训以应对AI冲击。苹果CEO库克也松口，受全球内存短缺影响，未来产品涨价“不可避免”，可能波及iPhone 17系列。

2. **芯片与封装技术**：台积电正加速研发CoPoS面板级封装技术，用玻璃基板替代传统晶圆，可降低成本20-30%，计划2028年量产，应对AI芯片需求。SK海力士大规模招聘设计工程师，引发中小芯片企业不安。

3. **科技公司动态**：马斯克全额行权2018年特斯拉薪酬方案，账面收益达1160亿美元。Telegram在印度上诉失败，政府封禁决定获法院支持。努比亚将集中资源推第二代豆包AI手机，收缩传统手机业务。

4. **宇宙与AI研究**：最新研究确认宇宙仍在加速膨胀，暗能量“危机”解除。另有报道指出，AI快速学会宇宙法则后，反而可能“看不见”新物理现象。

5. **其他**：LINE网页版将终止服务；Adobe Firefly AI助手大升级；FIFA内部系统被曝安全漏洞等。

你对哪条新闻更感兴趣？我可以展开讲讲。
[root@iZbp12zrjuiewhsqxvgm4yZ personal_assistant]# 
```