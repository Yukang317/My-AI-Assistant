# 开发日志 — 协作模式升级：骨架式教学开发（Mode E）

**日期**：2026-06-03
**主题**：优化协作流程 + 精简项目文档

---

## 做了什么

### 1. 正式定义 Mode E（骨架式教学开发）

用户一直偏好一种"Claude 生成代码骨架 → 逐函数教学 → 用户实现 → 互动问答 → 收尾记录"的协作方式，但之前不稳定触发。

**解决方案**：将这种模式正式写为 CLAUDE.md 中的 **Mode E（骨架式教学开发）**，明确定义 4 步强制流程，设为 `personal_assistant/` 项目的默认模式。

### 2. 精简 CLAUDE.md（295 行 → 265 行）

| 精简点 | 改动 |
|--------|------|
| SAGT 架构描述 | 3 个独立章节合并为 1 个紧凑的 Architecture 章节 |
| 启动时需读文件 | 10 个 → 3 个核心文件（dev_log 按需读取） |
| RAG Architecture + Tech Stack | 合并为 1 段 |
| LLM Layer | 修掉啰嗦描述 |

### 3. 创建用户偏好记忆

写入了 `skeleton-teaching-preference.md` memory，跨会话持久化 Mode E 偏好。

---

## 踩坑

无代码 bug。纯粹流程/文档优化。

## 概念卡片

- **CLAUDE.md 置顶原则**：想让 Claude 稳定遵守的行为，必须写入每次会话都加载的指令文件（CLAUDE.md / Memory），不能依赖"它上次记得"
- **IP vs COSINE**：BGE 用 `normalize_embeddings=True` 做 L2 归一化后，向量模长=1，此时 IP（内积）等价 COSINE（余弦相似度），但快一倍——Milvus 必须用 IP
