# 阶段 6.3 — H2 结构化追踪验收完成

**日期**：2026-06-25
**模式**：Mode F（对照参考教学）+ 验收复审
**内容**：`agent/trace.py` + `state.py` + `main_graph.py` 双层追踪接入，H2 验收通过

---

## 〇、做了什么 & 为什么

阶段 6.3 Harness 的最后一项 H2：让每次 `/chat/graph` 请求可追溯。

- **层 1 LangSmith**：配 `.env` 后官方自动 trace（graph + LLM）
- **层 2 本地 JSONL**：`logs/agent_trace.jsonl`，离线 grep，记录业务字段（intent/tool/bound/latency）

H1（安检）+ H3（黄金路径）已在 6/22 完成；本次把 H2 从「trace.py 写完」推进到「main_graph 全链路打点 + 验收」。

---

## 一、新增/修改的文件

| 文件 | 变更 |
|------|------|
| `agent/trace.py` | 新建：TraceEvent、log_event、5 个 trace_* 函数、agent_trace_context |
| `agent/state.py` | 新增 `StateField.TRACE_ID` + `trace_id: NotRequired[str]` |
| `agent/main_graph.py` | `_invoke_with_trace` 统一入口；intent_node / tool_execute 打点 |

**JSONL 事件链**（闲聊路径）：
```
run_start → route_decision → run_end
```
工具路径额外有：`bound_check` → `tool_execute`

---

## 二、验收过程 & 踩坑

### 第一轮验收（未通过）

| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| 1 | `trace.py` 无法 import | `except ImportError` 分支的 `@contextmanager` 函数缺 `yield` | 补 `yield` |
| 2 | `StateField.TRACE_ID` 不存在 | 枚举漏加，只写了 TypedDict 字段 | 补 `TRACE_ID: str = "trace_id"` |
| 3 | `main_graph.py` SyntaxError | `_invoke_with_trace` 写成 `- > dict` | 改为 `-> dict` |
| 4 | JSONL 只有 run_start/end | import 了 trace 函数但 intent_node/tool_execute 未调用 | 对照 `_ref_main_graph.py` 接入 |

### 第二轮验收（通过）

```bash
uv run python -m py_compile agent/trace.py agent/state.py agent/main_graph.py  # OK
PYTHONPATH=. uv run python test/agent_paths_test.py  # exit 0，4 pass + 1 KNOWN
tail -5 logs/agent_trace.jsonl  # 完整事件链
```

`_ref_trace.py`、`_ref_state.py`、`_ref_main_graph.py` 已删除。

---

## 三、关键决策（同次会话前后讨论）

| 决策 | 结论 |
|------|------|
| H4 路由硬编码修 case 5 | **搁置**——个人资料将进知识库，需统一 Context Engineering 方案 |
| LangSmith vs 自写 JSONL | **双层互补**，不是二选一 |
| 完整后端工程（权限/多用户/并发） | **远期**；当前单人自用，H2 轻量切片够用 |
| H2 完成后下一步 | **阶段 6.4 while-true 多轮 loop**（灵感引擎前置） |

---

## 四、文件状态

| 文件 | 状态 |
|------|------|
| `agent/trace.py` | ✅ |
| `agent/state.py` | ✅ |
| `agent/main_graph.py` | ✅ H1+H2 |
| `test/agent_paths_test.py` | ✅ exit 0 |

---

## 五、下一步

**阶段 6.4 Loop Engineering**：
- L1 while-true 多轮工具循环（max_turns 硬限制）
- L2 工具结果观察 → 决定是否继续
- L3 预算控制（token / 调用次数 / 超时）

完成后进入 **6.5 灵感引擎 Phase 1**。
